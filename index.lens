<koken:include file="inc/header.html" />

<div id="content">

	<koken:load>

		<!-- Slideshow that loads template data (Featured Content by default) -->
		<koken:pulse size="3:2" autostart="true" timer:enabled="true" transitions:type="dissolve" />

	<koken:else>
		
		<!-- Fallback content if data load contains no data -->
		No featured or favorite content found. Assign either in the Library then edit the page filter if necessary.

	</koken:load>

</div>

<koken:include file="inc/footer.html" />